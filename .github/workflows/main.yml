name: CI

on:
  push:
    branches: "*"
  pull_request:
    branches: "*"
  schedule:
    - cron: "0 0 * * *"

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.6', '3.7', '3.8']
    name: Python ${{ matrix.python-version }} build
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      - name: Install Python
        uses: actions/setup-python@v1
        with:
          python-version: ${{ matrix.python-version }}
          architecture: "x64"
      - name: Install dependencies
        run: |
          # $CONDA is an environment variable pointing to the root of the miniconda directory
          $CONDA/bin/conda create -n dev -c conda-forge xarray dask pytest pytest-cov netcdf4 pip
          $CONDA/bin/conda activate dev
          python -m pip install .
      - name: Run tests
        run: |
          $CONDA/bin/conda activate dev
          pytest --junitxml=test-reports/junit.xml --cov=./ --verbose
      # TODO: Token required-> https://github.com/codecov/codecov-action
      # - name: Upload coverage to Codecov
      #   uses: codecov/codecov-action@v1
      #   with:
      #     token: ${{ secrets.CODECOV_TOKEN }} # Required
      #     file: ./coverage.xml
      #     flags: unittests
      #     name: codecov-umbrella
      #     yml: ./codecov.yml
